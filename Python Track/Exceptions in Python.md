When handling cases where a user in a python script may encounter an error, you can actually set up conditions where if a user prompts such an error, or an exception, a particular script does *x*. In the CS50 tutorial, the keywords try, except, and *else* were used to implement a scenario in which a user was prompted 

